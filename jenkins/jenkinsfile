pipeline {

    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'teststring'
                sh '''
                sudo apt-get install python3-pip -y
                '''
            }
        }
    }
}
